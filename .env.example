# docker-compose.yml ve docker-compose.microservices.yml için environment değişkenleri.
# Bu dosyayı kopyalayıp .env olarak kaydedin:
#   cp .env.example .env
# Ardından değerleri değiştirin.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=tourconnect

RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest
